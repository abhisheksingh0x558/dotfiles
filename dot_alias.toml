# Git
g = "git"
grm = "git rm"

# Just
make = "just"
j = "just"
jc = "just --choose"
jl = "just --list"
jg = "just --global-justfile"
jgc = "just --global-justfile --choose"
jgl = "just --global-justfile --list"

# Chezmoi
cz = "chezmoi"
cza = "chezmoi add"
czy = "chezmoi apply"
czd = "chezmoi diff"
czg = "chezmoi git"
czi = "chezmoi init"

# Zoxide
cd = "z"

# Eza
eza = "eza --all"
ls = "eza --all"

# Bat
cat = "bat"
man = "batman"

# Fd
fd = "fd --hidden"
find = "fd --hidden"

# Ripgrep
grep = "rg"

# Delta
diff = "delta"

# Sd
sed = "sd"

# Choose
awk = "choose"

# Rust parallel
xargs = "rust-parallel"

# Scc
wc = "scc"

# Hyperfine
time = "hyperfine"

# Procs
ps = "procs"

# Kubernetes
k = "kubectl"
# Kubernetes get
kg = "kubectl get"
kgd = "kubectl get deployments"
kgp = "kubectl get pods"
kgs = "kubectl get services"
# Kubernetes describe
kd = "kubectl describe"
kdd = "kubectl describe deployments"
kdp = "kubectl describe pods"
kds = "kubectl describe services"

# Neovim
vi = "nvim"
vim = "nvim"
