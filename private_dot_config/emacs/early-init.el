;; -*- lexical-binding: t -*-

;; Package manager
(setq package-enable-at-startup nil) ; Disable package loading at startup

;; UI/UX
(menu-bar-mode 0) ; Disable menu bar
(tool-bar-mode 0) ; Disable tool bar
(scroll-bar-mode 0) ; Disable scroll bar
